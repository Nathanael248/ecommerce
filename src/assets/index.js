// // Vite-friendly asset index: imports all image files under this folder (recursively)
// const modules = import.meta.glob('./**/*.{png,jpg,jpeg,svg,gif}', { eager: true });

// const images = {};
// for (const path in modules) {
//   const key = path.replace('./', '');
//   images[key] = modules[path].default ?? modules[path];
// }

// export default images;
